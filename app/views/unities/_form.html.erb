<%= form_with(model: unity) do |form| %>
  <% if unity.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(unity.errors.count, "error") %> prohibited this unity from being saved:</h2>

      <ul>
        <% unity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <p>
    <%= form.label :secretary_id %>

    </p>
    <%= form.collection_select :secretary_id, Secretary.all, :id, :name, prompt: "Selecione uma Secretaria" %>
  </div>

  

  <div>
    <%= form.submit %>
  </div>
<% end %>
