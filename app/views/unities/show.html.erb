<p style="color: green"><%= notice %></p>

<div id="list-obj">
  
  <%= render @unity %>

  <div id="body-list">
    <h2>Funcionários</h2>
    <% @employees.each do |employee| %>
      <p><%= employee.name%></p>
    <% end %>

    <h2>Atendentes</h2>
    <% @attendants.each do |attendant| %>
      <p><%= attendant.name%></p>
    <% end %>




  </div>



  <div id="btns-sec">
  <%= link_to "Editar unidade", edit_unity_path(@unity) %> |
  <%= link_to "Voltar para unidades", unities_path %>
  </div>
  <div id="btn-destroy">
    <%= button_to "Excluir unidade", @unity, method: :delete %>

  </div>

</div>
